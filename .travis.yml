language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm

services: mysql

before_script:
  - unzip src/Hope/RestBundle/Resources/tests/api_test_2014-08-04.sql.gz dump.sql
  - mysql -e 'create database app_test;'
  - mysql app_test < dump.sql
  - composer install -n

script: phpunit -c app
